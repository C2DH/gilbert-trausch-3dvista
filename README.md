# 3D Vista project for Trausch

Note: mdeia should be provided separately, as it is not included in the repository.

This is the result of the "publish" action on 3D Vista Virtual Tour software.
We modified the default index.html to include a custom script

```bash
./build.sh
```

Test locally with a server module,

```bash
python3 -m http.server
```

or

```bash
npx http-server
```
